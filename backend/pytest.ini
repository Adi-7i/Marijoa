[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: tests requiring real external services (PostgreSQL, Redis, Azure, LLM)
    slow: tests that take more than a few seconds
    external: tests requiring network access or paid external API calls
filterwarnings =
    ignore::DeprecationWarning:pydantic
    ignore::DeprecationWarning:sqlalchemy
